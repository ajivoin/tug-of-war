name: Lint

on:
  push:
    branches: [ main ]
    paths:
    - '**.js'
    - '**.ts'
    - '**.json'
  pull_request:
    branches: [ main ]
    paths:
    - '**.js'
    - '**.ts'
    - '**.json'

jobs:
  lint:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 24.x
        cache: npm
    - run: npm ci
    - run: npm run lint
    - run: npm run typecheck
    - run: npm test
